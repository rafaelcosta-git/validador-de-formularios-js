# 🧮 Validador de Formulários (JS)

Validação de **nome de utilizador**, **e-mail** e **senha** com **expressões regulares (Regex)** e mensagens no DOM.

### Tecnologias
HTML5 • CSS3 • JavaScript (ES6)

### Como executar
Abre o `index.html` no navegador.

### O que aprendi
- Aplicar Regex no JS
- Manipular o DOM para dar feedback ao utilizador
